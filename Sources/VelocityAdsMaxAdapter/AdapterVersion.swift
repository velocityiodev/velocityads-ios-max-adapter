/// Single source of truth for the adapter version string.
///
/// To release a new adapter version, update this constant and then bump the
/// version in `VelocityAdsMaxAdapter.podspec` (`s.version`) and `Package.swift`
/// to match. All three must stay in sync.
internal let velocityAdsMaxAdapterVersion = "0.10.0.0"
